import { createAction } from 'redux-actions';

export const INSTRUCTOR_COURSES_DEHYDRATE = 'INSTRUCTOR_COURSES_DEHYDRATE';

export const instructorCoursesDehydrate = createAction(INSTRUCTOR_COURSES_DEHYDRATE);
