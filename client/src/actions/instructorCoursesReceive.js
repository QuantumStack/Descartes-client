import { createAction } from 'redux-actions';

export const INSTRUCTOR_COURSES_RECEIVE = 'INSTRUCTOR_COURSES_RECEIVE';

export const instructorCoursesReceive = createAction(INSTRUCTOR_COURSES_RECEIVE);
